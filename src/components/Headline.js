export default ({ children }) => {
  return (
    <p
      css={{
        color: "#4CAF50",
        fontSize: "28px",
        textAlign: "center",
        lineHeight: "45px",
        marginBottom: "60px"
      }}
    >
      {children}
    </p>
  );
};
